# This YAML describes the Item structure appropriate for configuring
# the 'simp_lite' scenario. This structure is is used to generate
# an internal YAML object, which represents a decision tree.
# This YAML object is generated by concatenating yaml pieces
# listed in the 'includes' sequence below.
#
---
name: simp_lite
description: |
    Settings for a SIMP system in which some security features
    are disabled for SIMP clients.  The SIMP server will
    be running with all security features enabled.
includes:
- initial_actions
- network_setup
- general_server_setup
- puppet_setup
- yum_setup
- ldap_setup:
  - ldap_query_options: 'SKIPQUERY SILENT NOYAML'
  - sssd_query_options: 'SKIPQUERY SILENT NOYAML'
- misc_setup
- user_setup
